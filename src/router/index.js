import Vue from 'vue'
import VueRouter from 'vue-router'


Vue.use(VueRouter)

const routes = [
    // 登录首页 Login
    {
        path: '/login'
            // component:
    }

]

const router = new VueRouter({
    routes,

})

export default router